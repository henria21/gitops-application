image:
  repository: henria/weather_app_backend
  tag: "latest"
  pullPolicy: IfNotPresent

replicaCount: 1

service:
  type: ClusterIP
  port: 80
  targetPort: 5000

env:
  - name: FLASK_ENV
    value: production
